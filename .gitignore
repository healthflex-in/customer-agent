# Secrets
.env
.env.*
*.pem
*.key
config/

# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
.venv/
venv/
env/

# macOS
.DS_Store
._*

# Audio / media (runtime files)
received_audio/
tts_cache/
audio_files/
*.wav
*.mp3

# Transcripts (runtime data)
transcripts/

# Upload temp
output/

# ChromaDB (runtime vector store) — anchored to the DataHandling root so
# this pattern does NOT match app/db/ (which holds source code).
/DataHandling/db/

# Node
node_modules/

# IDE
.idea/
.vscode/

# Local-only refactor archive (dead code moved aside, not for git)
.trash/

# Local docker compose overrides (per-dev port mappings, etc.)
docker-compose.override.yml
