# Production requirements for Docker (no GUI dependencies)

# Core dependencies
numpy
pymongo
boto3
requests
Pillow

# FastAPI and Server
fastapi==0.115.9
uvicorn[standard]==0.23.2
python-dotenv==1.0.1
python-multipart

# WebSocket dependencies
websockets>=13.0.0,<15.1.0

# LlamaIndex Core (Gemini/Google GenAI only — Vertex removed, not used)
llama-index==0.12.25
llama-index-core==0.12.25
llama-index-llms-gemini==0.4.14
llama-index-llms-google-genai==0.1.12

# Google Cloud Services
google-generativeai==0.8.5
google-cloud-speech==2.31.1
google-auth==2.39.0
google-api-core==2.24.2

# Audio Processing
pydub==0.25.1
edge-tts>=6.1.9

# Networking and gRPC
grpcio>=1.71.0,<1.73.0

# OpenTelemetry (base only — vertexai/openai instrumentation removed)
opentelemetry-api==1.32.1
opentelemetry-sdk==1.32.1
opentelemetry-instrumentation-fastapi==0.53b1
openinference-instrumentation==0.1.28

# Brand RAG — Stance Health knowledge base (ChromaDB vector store)
chromadb==1.0.4

# Langfuse LLM observability
langfuse>=2.0.0
openinference-instrumentation-llama-index>=3.0.0

# LangGraph
langgraph>=0.2.0

# Utilities
tqdm==4.67.1
pdf2image

# Resilience / retry
tenacity>=9.1.2

# Observability
prometheus-client>=0.19.0
starlette-prometheus>=0.7.0
structlog>=25.2.0
asgi-correlation-id>=4.3.4

# Config
pydantic-settings>=2.8.1

# Removed (not used in production):
# - torch / torchaudio        (~1.4 GB) — audio/utils.py is never imported; STT now uses Google Cloud Speech
# - transformers              (~400 MB) — same, only in unused audio/utils.py
# - sentence-transformers     (~100 MB) — not imported anywhere
# - huggingface-hub           (pulled by above)
# - llama-index-embeddings-huggingface — needs torch; VectorStore never queried in graph path
# - llama-index-vector-stores-chroma / chromadb — initialized but never queried in active path
# - llama-index-llms-vertex   (~20 MB)  — not imported anywhere
# - google-cloud-speech       — re-added (STT backend switched from faster-whisper to Google Cloud STT)
# - google-cloud-aiplatform   (~200 MB) — not imported anywhere
# - arize-phoenix-*           (~100 MB) — not imported in production code
# - openinference-instrumentation-vertexai/openai — Vertex/OpenAI not used
